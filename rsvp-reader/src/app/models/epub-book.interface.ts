export interface EpubChapter {n  id: string;n  title: string;n  href: string;n  index: number;n  spineIndex?: number; // EPUB spine içindeki gerçek indexn  wordCount?: number;n  preview?: string; // Bölümün ilk kısımları (hover preview için)n  isRead?: boolean; // Kullanıcı tarafından okundu olarak işaretlendi min}nnexport interface EpubBook {n  title: string;n  author?: string;n  chapters: EpubChapter[];n  cover?: string;n  metadata?: {n    publisher?: string;n    language?: string;n    identifier?: string;n  };n  readChapters?: Set<number>; // Okunan bölüm indekslerin}nexport interface EpubReadingSession {n  bookInfo: EpubBook;n  currentChapter: number;n  fileName: string;n  loadedAt: Date;n  readChapters: Set<number>; // Okunan bölümlern}